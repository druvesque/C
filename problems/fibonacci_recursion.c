#include<stdio.h>

int fibonacci(int x) {
    if (x == 0) 
        return 0;
    else
        return 
}

int main() {
    int n;
    printf("Enter number of terms: ");
    scanf("%d", &n);
    fibonacci(n)
    return 0;
}
